# CsvStandardization

## Init Project
### Build Docker image
`cd CsvStandardization`
`docker build -t [image_name] .`

### Build React package
`cd static`
`npm run build`

### Start a python server
`docker-compose up`
